export const selectItems = ({ items }) => items;
export const selectItemName = ({ items }) => items.name;
export const selectItemLocation = ({ items }) => items.location;
export const selectLoading = ({ loading }) => loading.isLoading;
export const selectDeleting = ({ deliting }) => deliting;
